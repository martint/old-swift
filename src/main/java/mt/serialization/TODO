* Mechanism to report truncation / coercion errors
* Autoboxing support (setter/getters that accept Integer vs int, etc) 
* Bind bean to Thrift fields via annotations

Serializer
	* uses Protocol, Schema
	* defines mechanism for turning Object or Map into underlying protocol
	* works in terms of high-level types

Protocol
	* defines encoding of high-level types to wire types (int16, int32, int64, byte[], string, etc)
